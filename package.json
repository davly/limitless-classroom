{
  "name": "limitless-classroom",
  "version": "0.1.0",
  "description": "Limitless Classroom — Ofsted + DfE compliance gate for AI-assisted classroom-content emission.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/davly/limitless-classroom.git"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
